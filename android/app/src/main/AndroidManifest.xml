<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />

    <!--
        ตำแหน่งเป็นหัวใจของแอป ไม่ใช่ของเสริม ถ้าไม่ประกาศไว้ตรงนี้
        การขอสิทธิ์ตอนรันจะล้มเงียบๆ แล้วผู้ใช้จะเจอแค่ "หาตำแหน่งไม่ได้" โดยไม่มีกล่องขอสิทธิ์
        ต้องมีทั้งสองระดับ เพราะ Android 12 ขึ้นไปผู้ใช้เลือกให้เฉพาะตำแหน่งคร่าวๆ ได้
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- สั่นเตือนก่อนถึงจุดเลี้ยวและเมื่อเจอสิ่งกีดขวาง สำหรับตอนอยู่ในที่เสียงดัง -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- กันหน้าจอดับระหว่างนำทาง ถ้าจอดับการนำทางจะหยุดและผู้ใช้จะเดินต่อโดยไม่มีเสียงบอกทาง -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!--
        GPS เป็นเงื่อนไขบังคับ ไม่ใช่ตัวเลือก
        ประกาศไว้เพื่อไม่ให้เครื่องที่ไม่มี GPS ติดตั้งแอปนี้แล้วพบว่าใช้ไม่ได้เลย
    -->
    <uses-feature android:name="android.hardware.location.gps" android:required="true" />
</manifest>
